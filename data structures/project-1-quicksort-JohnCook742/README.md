# project-1-quicksort

Use the provided code to implement a quicksort.

You can change code in `sort.py`. Your code needs to pass all the tests in `sort-test.py`. Remember that I will review all code to make sure that the code is actually a quicksort.
