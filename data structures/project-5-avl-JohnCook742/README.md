# project-5-avl

Use the attached code to write implement a self-balancing AVL Abstract Data Type.

You can change code in `avl.py` and `node.py`. Your code should pass all the tests in `test.py`.
